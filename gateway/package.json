{
  "name": "@chan/stopwatch-devices-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types worker-configuration.d.ts",
    "check": "npm run types && tsc --noEmit",
    "test": "vitest run",
    "dry-run": "wrangler deploy --dry-run --outdir ../.build/gateway",
    "deploy": "npm run check && npm test && wrangler deploy"
  },
  "dependencies": {
    "twitter-text": "3.1.0"
  },
  "devDependencies": {
    "@types/twitter-text": "3.1.10",
    "typescript": "7.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.131.1",
    "@types/node": "22.20.2",
    "@cloudflare/vitest-plugin": "1.1.8"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
