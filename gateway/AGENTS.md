# Historical gateway snapshot

Active gateway source is `/Users/chan/Developer/chan-services/apps/devices` in
private `chantastic/chan-services`. Its `docs/deployment.md` records production
ownership. Use that workspace for gateway implementation, tests, and releases.

Preserve this directory's source/history; do not deploy it or maintain a second
gateway here. The parent repository remains the active StopWatch firmware project.
No service source migration calls for a firmware flash or device-data changes.
